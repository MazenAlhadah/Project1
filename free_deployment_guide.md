# دليل النشر المجاني بالكامل — منصة معادلات الهندسة

لرفع المنصة التعليمية أونلاين مجاناً 100% وبدون الحاجة لدفع أي تكاليف شهرية، سنستخدم أفضل وأقوى خيار متوفر للمطورين عالمياً:

1. **Vercel (لرفع الموقع بالكامل):** أفضل منصة سحابية مجانية وسريعة جداً لاستضافة مشاريع Next.js (الواجهة الخلفية والأمامية معاً).
2. **Neon.tech (لقاعدة البيانات):** منصة سحابية تمنحك قاعدة بيانات PostgreSQL سحابية وسريعة جداً ومجانية بالكامل (تتميز بخاصية التوقف التلقائي لتوفير الموارد لتبقى مجانية للأبد).
3. **Cloudinary (للملفات والكتب):** أنت تستخدمه بالفعل وهو مجاني بالكامل!

---

## 🛠️ الخطوة 1: إنشاء قاعدة البيانات المجانية على Neon.tech

1. اذهب إلى موقع [Neon.tech](https://neon.tech) وسجل حساباً مجانياً (باستخدام حساب GitHub أو البريد الإلكتروني).
2. بمجرد الدخول، انقر على **Create a Project** (إنشاء مشروع جديد).
3. اكتب اسماً لمشروعك (مثال: `engineering-platform`).
4. اختر إصدار PostgreSQL (الإصدار الافتراضي 16 أو 15 ممتاز جداً).
5. اختر موقع السيرفر الأقرب إليك (مثلاً `Europe (Frankfurt)` ممتاز جداً للشرق الأوسط).
6. انقر على زر **Create Project**.
7. ستظهر لك شاشة بها رابط الاتصال المباشر **Connection String**!
8. تأكد من تحديد خيار **Pooled** (لتحسين أداء الاتصال) من القائمة المنسدلة إن وجد، أو الخيار الافتراضي.
9. انسخ رابط الاتصال الذي سيظهر لك وسيكون بالشكل التالي تقريباً:
   ```text
   postgres://alex:pwd@ep-cool-darkness-123456.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```

---

## 💻 الخطوة 2: تهيئة الداتا بيز وجداول المشروع (Migration)

الآن سنقوم بتشغيل جداول داتا بيز المنصة على الداتا بيز الجديدة في Neon:

1. افتح ملف `.env.local` في جهازك مؤقتاً.
2. قم بتعديل متغيرات قاعدة البيانات لتشير إلى قيم Neon الجديدة بدقة كالتالي (يمكنك استخراجها بسهولة من الرابط الذي نسخته):
   * **DB_HOST**: الجزء الخاص بالسيرفر (مثال: `ep-cool-darkness-123456.eu-central-1.aws.neon.tech`)
   * **DB_PORT**: `5432`
   * **DB_USER**: اسم المستخدم (مثال: `alex` أو `postgres`)
   * **DB_PASSWORD**: كلمة المرور الموجودة في الرابط
   * **DB_NAME**: اسم الداتا بيز (عادة يكون `neondb`)
3. في مبنى الأكواد بجهازك، افتح الـ Terminal في مجلد `frontend` وقم بتشغيل الأوامر التالية لإنشاء الجداول وإضافة الأدمن الرئيسي:
   ```bash
   # تشغيل المايجريشن (إنشاء الجداول)
   npx sequelize-cli db:migrate
   
   # إضافة أول أدمن للنظام
   npx sequelize-cli db:seed:all
   ```
   *بمجرد اكتمال الأمرين بنجاح، أصبحت قاعدة بياناتك السحابية جاهزة وبها بيانات الأدمن الأساسي!*

---

## 🚀 الخطوة 3: رفع كود الموقع على GitHub

لرفع الموقع على Vercel، يجب أولاً رفعه على حساب GitHub الخاص بك (مجاني أيضاً):

1. أنشئ حساباً على [GitHub.com](https://github.com) إذا لم يكن لديك.
2. أنشئ مستودعاً جديداً (New Repository) واجعله **Private** (خاص).
3. في مجلد `frontend` بجهازك، افتح الـ Terminal واكتب الأوامر التالية لرفع الكود (مع التأكد من وجود ملف `.gitignore` لمنع رفع المتغيرات الحساسة):
   ```bash
   git init
   git add .
   git commit -m "Initial commit for production deployment"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   git push -u origin main
   ```

---

## 🌐 الخطوة 4: النشر والتشغيل مجاناً على Vercel

1. اذهب إلى موقع [Vercel.com](https://vercel.com) وسجل حساباً مجانياً باستخدام حساب **GitHub** الخاص بك.
2. انقر على **Add New...** ثم **Project**.
3. قم بعمل **Import** للمستودع الذي رفعته للتو من GitHub.
4. في صفحة الإعدادات، افتح قسم **Environment Variables** (متغيرات البيئة) وقم بإضافة المتغيرات التالية بدقة متناهية (انسخها من `.env.local` لديك):

| اسم المتغير (Key) | القيمة (Value) |
|---|---|
| `DB_HOST` | الرابط من Supabase (مثال: `db.xxxx.supabase.co`) |
| `DB_PORT` | `5432` |
| `DB_USER` | `postgres` |
| `DB_PASSWORD` | كلمة سر قاعدة البيانات في Supabase |
| `DB_NAME` | `postgres` |
| `JWT_ACCESS_SECRET` | كلمة سر تشفير الجلسات (سلسلة نصية طويلة وقوية) |
| `JWT_REFRESH_SECRET` | كلمة سر تشفير التحديث (سلسلة نصية طويلة وقوية) |
| `JWT_ACCESS_EXPIRY` | `15m` |
| `JWT_REFRESH_EXPIRY` | `7d` |
| `CLOUDINARY_CLOUD_NAME` | اسم الـ Cloud الخاص بك في Cloudinary |
| `CLOUDINARY_API_KEY` | مفتاح Cloudinary الخاص بك |
| `CLOUDINARY_API_SECRET` | كلمة سر Cloudinary الخاص بك |
| `PORT` | `3000` |
| `NODE_ENV` | `production` |
| `NEXT_PUBLIC_API_URL` | `/api` |
| `NEXT_PUBLIC_PLATFORM_NAME` | اسم منصتك التعليمية |

5. انقر على زر **Deploy** الأزرق الكبير! 🚀

---

## 🎉 مبروك! موقعك الآن أونلاين ومتاح للجميع

بعد دقيقة واحدة، سينتهي Vercel من بناء المشروع وسيمنحك رابطاً مجانياً ومؤمناً بشهادة SSL تلقائية (مثل: `https://your-project.vercel.app`).

### ⚙️ ربط دومين خاص مجاناً (اختياري):
إذا كان لديك دومين خاص بك (مثال `myplatform.com`):
1. اذهب إلى إعدادات مشروعك في Vercel ثم **Domains**.
2. اكتب اسم دومينك وانقر **Add**.
3. قم بتعديل إعدادات الـ DNS في الشركة التي اشتريت منها الدومين لتشير إلى خوادم Vercel (سيظهر لك موقع Vercel القيم التي يجب إضافتها بدقة).

---
*هذا الدليل يمنحك منصة سحابية فائقة السرعة، دائمة ومجانية بنسبة 100% ومحمية بالكامل!*
